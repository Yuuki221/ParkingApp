<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="xuechan.parkingapp2">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Login"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".TimeLocSet"
            android:label="@string/title_activity_time_loc_set"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="xuechan.parkingapp2.TimeLocSet" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name=".RegisterPage"
            android:label="@string/title_activity_register_page"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="xuechan.parkingapp2.RegisterPage" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name=".PickTimeLocDuration"
            android:label="@string/title_activity_pick_time_loc_duration"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="xuechan.parkingapp2.PickTimeLocDuration" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name=".PrvParkInfo"
            android:label="@string/title_activity_prv_park_info"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="xuechan.parkingapp2.PrvParkInfo" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyAAioe4ULWDnyqxvFJYkMO9dYffSGxIvvQ" />

        <activity
            android:name=".ChooseYourParkingLocation"
            android:label="@string/title_activity_choose_your_parking_location">
            <intent-filter>
                <action android:name="xuechan.parkingapp2.ChooseYourParkingLocation" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ParkingInfo01"
            android:label="@string/title_activity_parking_info01"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ParkingInfo02"
            android:label="@string/title_activity_parking_info02"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ParkingInfo03"
            android:label="@string/title_activity_parking_info03"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ParkingInfo04"
            android:label="@string/title_activity_parking_info04"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ProvideYourParkingInfo"
            android:label="@string/title_activity_provide_your_parking_info"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>
